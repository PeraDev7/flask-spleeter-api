services:
  - type: web
    name: flask-spleeter-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "./start.sh"
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 10000
